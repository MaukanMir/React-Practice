import React from 'react'

const Visualizer = () => {
    const matrix = new Array(50).fill(0).map(() => new Array(16).fill(Infinity));

  return (
    <div>

    </div>
  );
}

export default Visualizer