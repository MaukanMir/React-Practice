import React from 'react'


const CodingChallenge11 = ({label, icon, number}) => {
  return (
    <div>

    <span>{icon}</span>
    <strong>{label}</strong>
    <span>{number}</span>

    </div>
  )
}

export default CodingChallenge11